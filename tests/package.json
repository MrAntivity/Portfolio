{
  "name": "portfolio-tests",
  "private": true,
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "cd .. && node --test tests/trip-model.test.cjs tests/lectures.test.cjs && node --experimental-vm-modules --test tests/trip-ui.test.cjs",
    "rules": "cd .. && firebase emulators:exec --only firestore,storage --project demo-travel-journal --config firebase.trip.json \"node --test tests/trip-rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "13.35.1",
    "jsdom": "30.1.0"
  }
}
